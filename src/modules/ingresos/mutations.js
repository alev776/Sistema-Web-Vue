export function setIngresos(state, ingresos) {
    state.ingresos = ingresos;
}

export function setError(state, error) {
    state.error = error
}

export function setErrors(state, error) {
    state.errorMessage = error
}